version: 2.1

orbs:
  python: circleci/python@0.2.1

jobs:
  build-and-test:
    executor: python/default
    steps:
      - run: jupyter-nbconvert --execute ./Image.ipynb
      - store_artifacts:
      path: ./Image.html

workflows:
  main:
    jobs:
      - build-and-test
